import { StacksTransaction } from '@stacks/transactions-v6/dist/transaction';
import { StacksPrivateKey, StacksPublicKey } from '@stacks/transactions-v6/dist/keys';
import { SpendingConditionOpts } from '@stacks/transactions-v6/dist/authorization';
export declare class TransactionSigner {
    transaction: StacksTransaction;
    sigHash: string;
    originDone: boolean;
    checkOversign: boolean;
    checkOverlap: boolean;
    constructor(transaction: StacksTransaction);
    static createSponsorSigner(transaction: StacksTransaction, spendingCondition: SpendingConditionOpts): TransactionSigner;
    signOrigin(privateKey: StacksPrivateKey): void;
    appendOrigin(publicKey: StacksPublicKey): void;
    signSponsor(privateKey: StacksPrivateKey): void;
    getTxInComplete(): StacksTransaction;
    resume(transaction: StacksTransaction): void;
}
