import { ClarityType } from '@stacks/transactions-v6/dist/clarity/constants';
interface BufferCV {
    readonly type: ClarityType.Buffer;
    readonly buffer: Uint8Array;
}
declare const bufferCV: (buffer: Uint8Array) => BufferCV;
declare const bufferCVFromString: (str: string) => BufferCV;
export { BufferCV, bufferCV, bufferCVFromString };
