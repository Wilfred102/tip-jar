import { AddressVersion } from '@stacks/connect/node_modules/@stacks/transactions/dist/constants';
import { privateKeyToAddress, publicKeyToAddressSingleSig } from '@stacks/connect/node_modules/@stacks/transactions/dist/keys';
import { AddressString, ContractIdString } from '@stacks/connect/node_modules/@stacks/transactions/dist/types';
export type AddressRepr = {
    hash160: string;
    contractName?: string;
} & ({
    version: AddressVersion;
    versionChar: string;
} | {
    version: AddressVersion;
} | {
    versionChar: string;
});
export declare function parse(address: AddressString | ContractIdString): AddressRepr;
export declare function stringify(address: AddressRepr): string;
export declare const fromPrivateKey: typeof privateKeyToAddress;
export declare const fromPublicKey: typeof publicKeyToAddressSingleSig;
